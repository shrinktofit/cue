{
  "name": "@bsgames/cue-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [
    "bin.js",
    "lib",
    "!lib/**/*.map"
  ],
  "bin": {
    "cue": "./bin.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "node --run build && vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bsgames/cue-compiler": "workspace:*",
    "yargs": "^18.1.0"
  },
  "devDependencies": {
    "@bsgames/cue-workflow": "workspace:*",
    "@types/node": "catalog:",
    "@types/yargs": "^17.0.35",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
